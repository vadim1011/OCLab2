<Window x:Class="OCLab2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:OCLab2.ViewModels"
        Title="OCLab2 - Parallel.ForEach Факториалы" 
        Height="600" Width="800"
        WindowStartupLocation="CenterScreen">
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Загрузить файл" 
                    Command="{Binding LoadFileCommand}"
                    Padding="20,10" Margin="0,0,10,0"/>
            <Button Content="Вычислить факториалы (Parallel.ForEach)" 
                    Command="{Binding CalculateCommand}"
                    Padding="20,10" 
                    Background="LightGreen"/>
        </StackPanel>

        <TextBlock Grid.Row="1" 
                   Text="{Binding FilePath, StringFormat='Файл: {0}'}" 
                   Foreground="Gray"
                   Margin="0,0,0,5"/>

        <!-- Статус -->
        <TextBlock Grid.Row="2" 
                   Text="{Binding Status}" 
                   FontWeight="Bold" 
                   FontSize="14"
                   Foreground="DarkBlue"
                   Margin="0,0,0,10"/>

        <GroupBox Grid.Row="3" Header="Результаты (число → факториал)">
            <DataGrid ItemsSource="{Binding Results}" 
                      AutoGenerateColumns="False"
                      IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Число" Binding="{Binding Key}" Width="80"/>
                    <DataGridTextColumn Header="Факториал" Binding="{Binding Value}" Width="*">
                        <DataGridTextColumn.ElementStyle>
                            <Style>
                                <Setter Property="TextBlock.TextWrapping" Value="Wrap"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>
    </Grid>
    </Window>
